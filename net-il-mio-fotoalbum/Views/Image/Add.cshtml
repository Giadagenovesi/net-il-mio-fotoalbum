@model ImageFormModel


<div class="container">
    <div class="row">
        <div class="col-12 mt-5">
            <h1 class="mb-3">Pagina creazione di una Foto</h1>
        </div>
        <form asp-controller="Image" asp-action="Add" enctype="multipart/form-data" method="post">
            @Html.AntiForgeryToken()
            <div class="mb-3">
                <strong>Titolo:</strong>
                <input type="text" class="form-control" asp-for="Image.Title">
                <span asp-validation-for="Image.Title" class="text-danger"></span>
            </div>
            <div class="mb-3">
                <strong>Descrizione:</strong>
                <input type="text" class="form-control" asp-for="Image.Description">
                <span asp-validation-for="Image.Description" class="text-danger"></span>
            </div>
            <!-- SELECT MULTIPLA PER  LE CATEGORIE -->
            <div class="mb-3">
                <strong>Seleziona le categorie:</strong>
                <select asp-for="SelectedCategoriesId"
                        multiple="multiple"
                        size="10"
                        class="form-control mb-2 mr-sm-2 ml-sm-3 leftList"
                        asp-items="@Model.Categories">
                </select>
            </div>

            <div class="mb-3">
                <strong>URL Immagine:</strong>
                <input type="text" class="form-control" asp-for="Image.ImageUrl">
                <span asp-validation-for="Image.ImageUrl" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="ImageFormFile">Image file</label>
                <input type="file" asp-for="ImageFormFile"/>
                <span asp-validation-for="ImageFormFile" class="text-danger"></span>
            </div>
            <button type="submit" class="btn my_btn mb-5">Crea</button>
        </form>
    </div>
</div>
